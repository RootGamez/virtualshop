# @virtualshop/api

Worker Hono + TypeScript. Pendiente de implementar en la **Fase 2** del plan (ver `/PLAN.md` en la raíz del repo):

- Migraciones D1 (`users`, `categories`, `products`, `product_variants`, `product_media`, `landing_config`, `whatsapp_config`, `events`)
- Auth JWT + middleware de roles (`owner` / `admin`)
- Endpoints REST descritos en la especificación §8
